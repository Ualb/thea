
#include "../constants.h"

/**
 * The algoritm create a DOC without SHEET's
 *
 * @param name The name of the doc
 * @param location Dir to save destiantion
 * @return Is or not created
 */
boolean createDOC(const char* name, char* location);